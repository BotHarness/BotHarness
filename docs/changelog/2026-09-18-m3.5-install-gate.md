---
title: M3.5 install verification gate
date: 2026-09-18T16:00:00+08:00
tags: [milestone, spec]
---

## Spec v1.4

- New milestone **M3.5 — install verification gate** between M3 (roster & delegation) and M4 (demo loop): run DSH locally, install the `deepseekbot` bundle through the official `dsh plugin` CLI (no dev link, no test mock), and confirm the plugin loads, the UI renders (`main` panel / `sidebar.panellist` / settings), and PersonaBot flows still work after a restart. M4 is now blocked by it.
- Issue map: #24 blocked by #10; #11 blocked by #24; roadmap hub updated in #20.

## Install research

- `docs/research/2026-09-18-dsh-plugin-installation.md`: `dsh plugin --profile <p> <args...>` forwards to pnpm inside the profile directory; bundles declare `dsh.bundle.patch` and land in `dsh.profile.bundles`; DSH ships a plugin manager (sidebar Plugins page + read-only settings list) but has no official marketplace and no `/plugin` command; bundle membership changes need a restart.
- Gate environment: pin `@deepseek-ai/dsh@0.1.5-rc.2`, isolated `DSH_HOME`, a dedicated profile cloned from the `web` template, install via the official CLI or a tarball — third-party market plugins stay out of the gate.

Upstream docs: <https://deepseek-harness.github.io/deepseek-harness/develop/basic/publish>.
